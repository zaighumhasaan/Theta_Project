@model IEnumerable<Pharmacy_POS.Models.SaleInvoice>

@{
    ViewData["Title"] = "All_Sale_Invoices";
}

<h1>All_Sale_Invoices</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table" id="MyTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Discount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NewPrice)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Emp)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Discount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NewPrice)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.Customer.CustomerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Emp.EmpId)
            </td>*@
            <td>
                <a asp-action="Edit" asp-route-id="@item.SaleInvoiceId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SaleInvoiceId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SaleInvoiceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
